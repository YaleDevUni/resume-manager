version: '3.8'

services:
  mongodb:
    image: mongodb/mongodb-community-server:7.0.3-ubuntu2204
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: always

volumes:
  mongodb_data:
